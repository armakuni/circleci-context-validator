---
version: 2.1

orbs:
  release-management: salesforce/npm-release-management@4.37.0

workflows:
  version: 2

  test-and-release:
    jobs:
      - release-management/test-package:
          matrix:
            parameters:
              os:
                - linux
              node_version:
                - latest
                - lts
                - v16 #developed with
      - release-management/release-package:
          context: context-validator-ci
          dryrun: true
          github-release: true
          node_version: v16
          filters:
            branches:
              only: main
          requires:
            - release-management/test-package-lts-linux
            - release-management/test-package-latest-linux
            - release-management/test-package-v16-linux


  dependabot-automerge:
    triggers:
      - schedule:
          cron: '0 2,5,8,11 * * *'
          filters:
            branches:
              only:
                - main
    jobs:
      - release-management/dependabot-automerge:
          context: context-validator-ci
